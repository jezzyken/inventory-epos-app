{
  "name": "inventory-epos-app",
  "version": "1.0.0",
  "description": "inventory app API",
  "main": "app.js",
  "scripts": {
    "dev": "nodemon app.js",
    "setup-production": "npm run install-frontend && npm run build-frontend && npm install",
    "install-frontend": "cd frontend/vue2 && npm install",
    "build-frontend": "cd frontend/vue2 && npm run build"
  },
  "author": "jezrelljolampong",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
